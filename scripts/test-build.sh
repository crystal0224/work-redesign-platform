#!/bin/bash

# 🔧 배포 전 빌드 테스트 스크립트
# 로컬에서 프로덕션 빌드가 성공하는지 확인합니다.

set -e  # 에러 발생 시 스크립트 중단

echo ""
echo "🚀 ============================================"
echo "   배포 전 빌드 테스트 시작"
echo "============================================"
echo ""

# 프로젝트 루트로 이동
cd "$(dirname "$0")/.."
PROJECT_ROOT=$(pwd)

# 색상 정의
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# 빌드 시작 시간
START_TIME=$(date +%s)

echo -e "${BLUE}📁 프로젝트 루트: ${PROJECT_ROOT}${NC}"
echo ""

# ============================================
# 1. Backend 빌드 테스트
# ============================================
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}📦 1/2: Backend 빌드 테스트${NC}"
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

cd "${PROJECT_ROOT}/backend"

# 의존성 체크
if [ ! -d "node_modules" ]; then
  echo -e "${YELLOW}⚠️  node_modules가 없습니다. npm install을 실행합니다...${NC}"
  npm install
  echo ""
fi

# TypeScript 빌드
echo -e "${BLUE}🔨 TypeScript 컴파일 중...${NC}"
npm run build

if [ $? -eq 0 ]; then
  echo ""
  echo -e "${GREEN}✅ Backend 빌드 성공!${NC}"
  echo -e "${GREEN}   → dist/ 디렉토리에 빌드 결과 생성됨${NC}"
else
  echo ""
  echo -e "${RED}❌ Backend 빌드 실패${NC}"
  echo -e "${RED}   → 위 에러 메시지를 확인하세요${NC}"
  exit 1
fi

echo ""

# ============================================
# 2. Frontend 빌드 테스트
# ============================================
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}📦 2/2: Frontend 빌드 테스트${NC}"
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

cd "${PROJECT_ROOT}/frontend"

# 의존성 체크
if [ ! -d "node_modules" ]; then
  echo -e "${YELLOW}⚠️  node_modules가 없습니다. npm install을 실행합니다...${NC}"
  npm install
  echo ""
fi

# Next.js 빌드
echo -e "${BLUE}🔨 Next.js 프로덕션 빌드 중...${NC}"
echo -e "${YELLOW}   (이 과정은 1-2분 소요될 수 있습니다)${NC}"
echo ""
npm run build

if [ $? -eq 0 ]; then
  echo ""
  echo -e "${GREEN}✅ Frontend 빌드 성공!${NC}"
  echo -e "${GREEN}   → .next/ 디렉토리에 빌드 결과 생성됨${NC}"
else
  echo ""
  echo -e "${RED}❌ Frontend 빌드 실패${NC}"
  echo -e "${RED}   → 위 에러 메시지를 확인하세요${NC}"
  exit 1
fi

# ============================================
# 완료
# ============================================
END_TIME=$(date +%s)
DURATION=$((END_TIME - START_TIME))

echo ""
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${GREEN}🎉 모든 빌드 테스트 성공!${NC}"
echo -e "${YELLOW}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""
echo -e "${BLUE}📊 빌드 통계:${NC}"
echo -e "   • Backend: ✅ 성공"
echo -e "   • Frontend: ✅ 성공"
echo -e "   • 소요 시간: ${DURATION}초"
echo ""
echo -e "${GREEN}✨ 배포 준비 완료! Railway와 Vercel에 배포할 수 있습니다.${NC}"
echo ""
