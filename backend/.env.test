# Test Environment Configuration
NODE_ENV=test
PORT=4001

# Database (will be overridden by test containers)
DATABASE_URL=postgresql://test_user:test_password@localhost:5433/work_redesign_test

# Redis (will be overridden by test containers)
REDIS_URL=redis://localhost:6380

# AI Configuration (Mock)
ANTHROPIC_API_KEY=test_key_mock_only
CLAUDE_MODEL=claude-3-sonnet-20240229

# Authentication
JWT_SECRET=test_jwt_secret_key_for_testing_only
JWT_EXPIRES_IN=1h

# File Upload
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=docx,xlsx,pdf,txt

# Email (Mock)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test_password
EMAIL_FROM=test@workredesign.sk.com

# AWS (Mock)
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=us-east-1
S3_BUCKET=test-work-redesign-bucket

# Logging
LOG_LEVEL=error
ENABLE_DEBUG_LOGS=false

# Rate Limiting (Relaxed for testing)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Session Configuration
SESSION_SECRET=test_session_secret
SESSION_COOKIE_MAX_AGE=86400000

# CORS
CORS_ORIGIN=http://localhost:3000